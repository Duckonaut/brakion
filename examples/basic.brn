fn main() {
    var x = 0;

    while (x < 10) {
        std::io::println(x.to_str());
        x = x + 1;
    }

    for (var i = 1; i <= 10; i = i + 1) {
        std::io::println(i.to_str());
    }
}
