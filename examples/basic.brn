fn main() {
    var x: u64 = 0;

    while (x < 10) {
        std::io::println(x.to_str());
        x = x + 1;
    }

    for i in std::iter::range(0, 10) {
        std::io::println(i.to_str());
    }
}
