fn f(x: bool | i32 ? bool, y: bool | i32) -> i32 {
    if x {
        return 1;
    }
    return 0;
}

fn f(x: bool | i32 ? i32, y: bool | i32 ? i32) -> i32 {
    return x as i32;
}

fn b() {
    f(true, true);
}
