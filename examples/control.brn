fn main(args: [str]) -> i32 {
    if true {
        std::io::println("something");
    }

    for a in args {
        std::io::println("arg: " + a);
    }

    return 0;
}

mod std {
    mod io {
        fn println(x: str) {
            # empty
        }
    }
}
